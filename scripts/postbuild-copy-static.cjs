/**
 * postbuild-copy-static.cjs
 *
 * ARCHITECTURAL NOTE:
 *
 * As of 2026-03-30, this script is a no-op.
 *
 * Pre-rendered HTML for SPA-owned routes (universe pages, blog posts) was removed
 * because it caused content mismatch on hard refresh: Vercel served the static HTML
 * first (simplified preview), then React hydrated and rendered the full interactive
 * experience → visible flash/swap.
 *
 * Architecture now: SPA (React Router) owns ALL app routes. No pre-rendered HTML.
 * One route = one renderer = one UX. Consistent experience on refresh and navigation.
 *
 * The blog post JSON data (public/blog/data/*.json) is generated by
 * generateBlogIndex.js which runs during `npm run build` BEFORE this script.
 *
 * This script is kept as a placeholder in case we need to add static asset
 * copying for genuinely static routes in the future.
 */
console.log('[postbuild] No static HTML copying needed — SPA owns all app routes.')
